# Optional: LLM paraphrase of the coaching insights (never chooses the beta).
# Leave unset to use the built-in rule-based coaching (what the demo uses).
XAI_API_KEY=
XAI_MODEL=grok-4-fast
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
